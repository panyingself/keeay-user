<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<!-- 不使用namespace的话sql搜索定位会比较方便 -->
<mapper namespace="com.keeay.anepoch.user.service.dao.permissioninfo.PermissionInfoMapper">
	<resultMap id="permissionInfoResultMap" type="com.keeay.anepoch.user.service.model.PermissionInfo">
        <result property="id" column="id"/>
        <result property="permissionCode" column="permission_code"/>
        <result property="permissionName" column="permission_name"/>
        <result property="uri" column="uri"/>
        <result property="createTime" column="create_time"/>
        <result property="createUser" column="create_user"/>
        <result property="updateTime" column="update_time"/>
        <result property="updateUser" column="update_user"/>
	</resultMap>
	<!-- 用于select查询公用抽取的列 -->
	<sql id="PermissionInfoColumns">
	    <![CDATA[
		id,permission_code,permission_name,uri,create_time,create_user,update_time,update_user
	    ]]>
	</sql>
	<!--sql where查询条件定义-->
	<sql id="listPermissionInfoWhere">
		<!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
		<where>
			<if test="id!=null">
				AND id = #{id}
			</if>
			<if test="permissionCode!=null">
				AND permission_code = #{permissionCode}
			</if>
			<if test="permissionName!=null">
				AND permission_name = #{permissionName}
			</if>
			<if test="uri!=null">
				AND uri = #{uri}
			</if>
			<if test="createTime!=null">
				AND create_time = #{createTime}
			</if>
			<if test="createUser!=null">
				AND create_user = #{createUser}
			</if>
			<if test="updateTime!=null">
				AND update_time = #{updateTime}
			</if>
			<if test="updateUser!=null">
				AND update_user = #{updateUser}
			</if>
		</where>
	</sql>
	<!--sql limit查询条件定义-->
	<sql id="limit">
	<if test="_offset != null and _limit != null">
			limit #{_offset}, #{_limit}
		</if>
		<if test="_offset == null and _limit != null">
			limit #{_limit}
		</if>
		<if test="_offset == null and _limit == null">
			<include refid="max_limit"/>
		</if>
	</sql>

	<sql id="max_limit">
		limit 500
	</sql>

	<sql id="orderby">
		<choose>
			<when test='_orderby != null'>
				order by
				<foreach collection="_orderby" item="item" index="index" separator=",">
					${item}
					</foreach>
				</when>
			<otherwise>
				<include refid="orderById"/>
				</otherwise>
		</choose>
	</sql>

	<sql id="orderById">
		order by create_time desc
	</sql>

	<!-- useGeneratedKeys="true" keyProperty="xxx" for sqlserver and mysql -->
	<insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO permission_info
        <trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="id!=null">
				id,
			</if>
			<if test="permissionCode!=null">
				permission_code,
			</if>
			<if test="permissionName!=null">
				permission_name,
			</if>
			<if test="uri!=null">
				uri,
			</if>
			<if test="createTime!=null">
				create_time,
			</if>
			<if test="createUser!=null">
				create_user,
			</if>
			<if test="updateTime!=null">
				update_time,
			</if>
			<if test="updateUser!=null">
				update_user,
			</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
				<if test="id!=null">
						#{id,jdbcType=BIGINT},
				</if>
				<if test="permissionCode!=null">
						#{permissionCode,jdbcType=VARCHAR},
				</if>
				<if test="permissionName!=null">
						#{permissionName,jdbcType=VARCHAR},
				</if>
				<if test="uri!=null">
						#{uri,jdbcType=VARCHAR},
				</if>
				<if test="createTime!=null">
						#{createTime,jdbcType=TIMESTAMP},
				</if>
				<if test="createUser!=null">
						#{createUser,jdbcType=VARCHAR},
				</if>
				<if test="updateTime!=null">
						#{updateTime,jdbcType=TIMESTAMP},
				</if>
				<if test="updateUser!=null">
						#{updateUser,jdbcType=VARCHAR}
				</if>
        </trim>
	</insert>

	<update id="update" >
        UPDATE permission_info SET
		<trim suffixOverrides=",">
	        <if test="permissionCode!=null">
	        permission_code = #{permissionCode,jdbcType=VARCHAR} ,
	        </if>
	        <if test="permissionName!=null">
	        permission_name = #{permissionName,jdbcType=VARCHAR} ,
	        </if>
	        <if test="uri!=null">
	        uri = #{uri,jdbcType=VARCHAR} ,
	        </if>
	        <if test="createTime!=null">
	        create_time = #{createTime,jdbcType=TIMESTAMP} ,
	        </if>
	        <if test="createUser!=null">
	        create_user = #{createUser,jdbcType=VARCHAR} ,
	        </if>
	        <if test="updateTime!=null">
	        update_time = #{updateTime,jdbcType=TIMESTAMP} ,
	        </if>
	        <if test="updateUser!=null">
	        update_user = #{updateUser,jdbcType=VARCHAR} ,
	        </if>
		</trim>
        WHERE 
	        id = #{id} 
	</update>

	<!--逻辑删除记录-->
	<delete id="delete">
		UPDATE permission_info SET delete_flag = 1
		<include refid="listPermissionInfoWhere"/>
	</delete>

    <!--
    	分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
    	因为分页查询将传 offset,pageSize,lastRows 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->

	<!--全量查询-->
    <select id="list" resultMap="permissionInfoResultMap">
    	SELECT <include refid="PermissionInfoColumns" /> FROM permission_info
		<include refid="listPermissionInfoWhere"/>
    </select>

	<!--该方法适用于，获取的数据条数小于等于1 如有特殊需要自己实现-->
	<select id="getDetailById" resultMap="permissionInfoResultMap">
		SELECT <include refid="PermissionInfoColumns" />
		FROM permission_info
		where id = #{id}
	</select>

	<!--该方法适用于，获取的数据条数小于等于1 如有特殊需要自己实现-->
	<select id="listByPermissionCodes" resultMap="permissionInfoResultMap">
		SELECT <include refid="PermissionInfoColumns" />
		FROM permission_info
		where permission_code in
		<foreach collection="permissionCodes" item="code" open="(" separator="," close=")">
			#{code}
		</foreach>
	</select>
</mapper>

